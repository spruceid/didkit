[default.http]
# DIDKIT_HTTP_PORT
port = 3000
# DIDKIT_HTTP_ADDRESS
address = [127, 0, 0, 1]
# DIDKIT_HTTP_BODYSIZELIMIT
bodysizelimit = 2097152 # 2MiB

[default.issuer]
# DIDKIT_ISSUER_KEYS = '[<JWK>, <JWK>]'
